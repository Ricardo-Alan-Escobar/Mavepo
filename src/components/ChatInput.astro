<div class="p-3 border-t border-gray-200">
    <form id="chat-form" class="flex flex-col sm:flex-row gap-2">
      <input 
        type="text" 
        id="chat-input" 
        placeholder="Escribe tu pregunta aquí..." 
        aria-label="Mensaje"
        class="flex-1 px-3 py-2 border border-gray-300 rounded-full focus:outline-none focus:ring-2 focus:ring-red-400 focus:border-transparent w-full sm:w-auto"
      />
      <button 
        type="submit" 
        id="chat-submit"
        class="px-4 py-2 bg-red-500 text-white rounded-full hover:bg-red-600 transition-colors focus:outline-none focus:ring-2 focus:ring-red-400 w-full sm:w-auto"
      >
        <span>Enviar</span>
      </button>
    </form>
  </div>